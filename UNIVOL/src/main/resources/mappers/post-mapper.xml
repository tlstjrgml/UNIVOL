<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.univol.post.model.Mapper.PostMapper">

	<select id="selectAll" resultMap="postResultSet">

		select * from (
			select rownum as rn, A.* from(
				select p.*, u.user_name
				from post p
				join users u on p.user_id = u.user_id
				where p.p_type = 'V'
				order by
					<choose>
						<when test="sort == 'views_desc'">p.views DESC</when>
						<when test="sort == 'asc'">p.views ASC</when>
						<otherwise>p.p_date DESC</otherwise>
					</choose>
			) A
			where rownum &lt;= #{endRow}
		)
		where rn &gt;= #{startRow}
	</select>
	
	<select id="getListCount" resultType="_int">
		
		select count(*) from post
    	where p_Type='V'
	</select>
	
	<insert id="insertPost">
		insert into post
		values(SEQ_POST.nextval, #{pTitle}, #{contents}, 0, #{pDate}, #{pType}, #{userId}, default)
	</insert>
	
	<resultMap type="com.univol.post.model.vo.Post" id="postResultSet">
		<result column="p_number" property="pNumber"/>
		<result column="p_title" property="pTitle"/>
		<result column="contents" property="contents"/>
		<result column="views" property="views"/>
		<result column="p_date" property="pDate"/>
		<result column="p_type" property="pType"/>
		<result column="user_id" property="userId"/>
		<result column="p_status" property="pStatus"/>
		<result column="likes" property="likes"/>
		<result column="user_name" property="userName"/>
	</resultMap>

	<select id="selectOne" resultMap="postResultSet">
		select p.*, u.user_name
		from post p
			join users u on p.user_id = u.user_id
		where p_number = #{pNumber}
	</select>

	<select id="selectAllPost" resultType = "com.univol.post.model.vo.Post">
		select * from post
	</select>	
	
	<update id="rollbackPost">
		update post
		set p_status ='Y'
		where p_number = #{pNumber}
	</update>
	
	<update id="deletePost">
		update post
		set p_status = 'N'
		where p_number = #{pNumber}
	</update>
	
	<select id="searchPosts" resultMap="postResultSet">
		select * from(
			select rownum as rn, A.* from(
				select p.*, u.user_name
				from post p
				join users u on p.user_id = u.user_id
				where p.p_type = 'V'
				and (p.p_title like '%' || #{keyword} || '%'
					 or p.contents like '%' || #{keyword} || '%'
					 or u.user_name like '%' || #{keyword} || '%'
				     )
				order by
					<choose>
						<when test="sort == 'views_desc'">p.views DESC</when>
						<when test="sort == 'views_asc'">p.views ASC</when>
						<otherwise>p.p_date DESC</otherwise>
					</choose>
			)A
			where rownum &lt;= #{endRow}

		)
		where rn &gt;= #{startRow}


	</select>
	
	
	
	<select id="getSearchCount" resultType="_int">
		select count(*) from post p
		join users u on p.user_id = u.user_id
		where p.p_type = 'V'
		and (
			p.p_title like '%' || #{keyword} || '%'
			or p.contents like '%' || #{keyword} || '%'
			or u.user_name like '%' || #{keyword} || '%'
		)
	</select>
	
	
	<update id="updateViews" parameterType="_int">
		update post
		set views = views + 1
		where p_number = #{pNumber}

	</update>
	
	
</mapper>
